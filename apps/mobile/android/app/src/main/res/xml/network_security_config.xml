<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for SAFE Android.

  Cleartext HTTP is permitted only for local/emulator development hosts.
  All other traffic (staging, production) must use HTTPS.
  Set VITE_API_BASE_URL to a HTTPS staging/production URL for release builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Android emulator host alias and loopback for local dev builds -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
